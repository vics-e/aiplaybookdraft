# Wave 2 numbered-content treatment map

This is the approved implementation map for numbered teaching content. It does not authorise changes to activities, saved answers, specialist tools, page wording or page order.

| Treatment | Count | Pages and intent |
|---|---:|---|
| Filled white marker | 6 | 7 `s1-capacity`; 13 `s2-risk`; 17 `s2-checks`; 50 `s6-scorecard`; 52 `s6-strategic-questions`; 54 `s7-policy` — short priority lists whose items should remain visible. |
| Numbered accordion | 19 | 5 `s1-framework`; 10 `s2-ethics-responsibility`; 11 `s2-data-confidentiality`; 12 `s2-legal-responsibility`; 15 `s2-failure-modes`; 16 `s2-over-reliance`; 18 `s2-evidence-trail`; 19 `s2-red-team`; 25 `s3-first-agent`; 29 `s3-confidence`; 32 `s4-types`; 36 `s5-dividend`; 38 `s5-time-pricing`; 39 `s5-pricing`; 41 `s5-3v-framework`; 44 `s5-client-talk`; 46 `s6-days1-30`; 47 `s6-days31-60`; 48 `s6-days61-90` — dense explanations that benefit from progressive disclosure. |
| Horizontal 1–5 | 8 | 4 `s1-stages`; 9 `s1-human-loop`; 24 `s3-workflow-map`; 26 `s3-maturity`; 31 `s4-framework`; 34 `s4-progression`; 49 `s6-rhythm`; 53 `s6-four-stages` — genuine ordered sequences of three to five stages or steps. |

Horizontal connector lines are reserved for genuine paths and progressions. Page 31 keeps the horizontal four-part structure but omits the connector because Role, Task, Context and Constraints are prompt components rather than stages in a path. All accordion and horizontal disclosures start closed.

The following numbered-source pages retain their existing specialist presentation: 14 `s2-policy`, 21 `s3-difference`, 22 `s3-where-agents`, 28 `s3-controls`, 30 `s3-ai-workflow`, 56 `s7-checklist`, 57 `s7-tool-matrix`, and 60 `s7-glossary`.

The shared implementation lives in `src/app/components/NumberedContentTreatments.tsx`. Pages absent from its mapping continue through the existing render path.
